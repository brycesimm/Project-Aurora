<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:Aurora.Shared.Models;assembly=Aurora.Shared"
             xmlns:local="clr-namespace:Aurora"
             x:Class="Aurora.MainPage"
             BackgroundColor="#F0F0F0">

    <ScrollView>
        <VerticalStackLayout
            Padding="20"
            Spacing="25">

            <Frame Padding="15" CornerRadius="10" HasShadow="True">
                <VerticalStackLayout Spacing="10">
                    <Label
                        Text="Vibe of the Day: An Inspiring Story"
                        FontSize="Large"
                        FontAttributes="Bold"
                        TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource Secondary}}" />
                    <Image
                        Source="dotnet_bot.png"
                        Aspect="AspectFill"
                        HeightRequest="150"
                        HorizontalOptions="FillAndExpand" />
                    <Label
                        Text="In a heartwarming turn of events, a local community rallied together to transform an abandoned lot into a vibrant urban garden, fostering new connections and providing fresh produce for hundreds of families. This initiative proves that positive change is always possible."
                        FontSize="Medium"
                        TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}" />

                    <!-- Placeholder Reaction Button (Story A-01.4) -->
                    <Button 
                        x:Name="ReactionButton"
                        Text="Uplifted Me"
                        Clicked="OnReactionButtonClicked"
                        HorizontalOptions="Start">
                        <Button.ImageSource>
                            <FontImageSource 
                                FontFamily="MDI"
                                Glyph="&#xF02D5;"
                                Color="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Black}}" />
                        </Button.ImageSource>
                    </Button>
                </VerticalStackLayout>
            </Frame>

            <!-- Daily Picks List (AC 1, AC 2, AC 3) -->
            <CollectionView ItemsSource="{Binding DailyPicks}" x:DataType="local:MainPage">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:ContentItem">
                        <Frame Padding="15" CornerRadius="10" HasShadow="True" Margin="0,0,0,15">
                            <VerticalStackLayout Spacing="5">
                                <Label
                                    Text="{Binding Title}"
                                    FontSize="Medium"
                                    FontAttributes="Bold"
                                    TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource Secondary}}" />
                                <Label
                                    Text="{Binding Snippet}"
                                    FontSize="Small"
                                    TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
